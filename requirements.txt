gitpython
pooch
# Mints the registry files (see register.py). Pinned deliberately: mccode-antlr
# is the consumer of these registries, so a bad release must not be able to
# quietly change what gets minted. Needs >=0.29.1 for the IR-sidecar filter that
# keeps generated files out of a registry.
mccode-antlr>=0.29.1
